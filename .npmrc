# npm 11 bloquea los install scripts de dependencias por defecto.
# Estos paquetes los necesitan (binario nativo de bcrypt, engines de Prisma, resolvers napi).
allow-scripts=@prisma/engines,prisma,bcrypt,unrs-resolver,@scarf/scarf=false
